<div class="space-y-4">
  <div class="flex justify-between items-center">
    <h1 class="text-2xl font-bold">Edit Page</h1>
    <%= link_to 'Back', page_path(@page.slug), class: "text-blue-500 hover:text-blue-600" %>
  </div>

  <%= render "form", page: @page %>
</div>
