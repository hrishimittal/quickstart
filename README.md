Quickstart
===========

Quickstart is a skeleton Rails 4 app with all the basic stuff I need for apps, like user signup, login, Bootstrap, static pages (using high_voltage).

The app was initially generated using [Prelang](http://prelang.com/), and then additional bits added as I needed them.
